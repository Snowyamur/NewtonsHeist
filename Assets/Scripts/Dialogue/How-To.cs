using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HowTo : MonoBehaviour
{
    // Insert the "Dialogue_Canvas" prefab into the Scene
    
    // Insert any object into the scene
    // Attach "Dialogue_Trigger.cs" script to object
    // Under the Dialogue_Trigger script on the object, change "Name" and "Sentences" size to whatever
    // Add sentences as necessary
    // Make sure object is a trigger
    // 
    // Dialogue trigger happens only once (subject to change)
    
}
